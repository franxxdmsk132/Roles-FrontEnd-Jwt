import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eabmodedl.app',
  appName: 'crud-front',
  webDir: 'dist/crudFRONT',
};

export default config;
